# Jarvis

Starter scaffold for a modular assistant project.

## Structure

- `main.py`: entry point
- `core/`: planning, memory, tool registry, and agent loop
- `tools/`: drop-in tools
- `memory/`: logs and vector database storage
- `services/`: speech and OS helpers
- `config/`: settings and environment variables

## Run

```bash
python main.py
```
